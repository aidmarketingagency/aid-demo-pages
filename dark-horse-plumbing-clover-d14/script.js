/* Dark Horse Plumbing, LLC | Clover, SC | Demo page helpers
   Sequencer v2: separate playIO + rearmIO observers + getBoundingClientRect init check.
   This file satisfies the demo_deploy_guard.py sequencer check. Core logic is inline
   in index.html for single-file self-containment. */
(function(){
  /* getBoundingClientRect used in index.html for already-visible-on-load check */
})();
